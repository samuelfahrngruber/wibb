import { ApiResourcePipePipe } from './api-resource-pipe.pipe';

describe('ApiResourcePipePipe', () => {
  it('create an instance', () => {
    const pipe = new ApiResourcePipePipe();
    expect(pipe).toBeTruthy();
  });
});
